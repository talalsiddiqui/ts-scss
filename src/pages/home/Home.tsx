import React from 'react'
import { Button } from 'antd'
import { history } from '../../routing/history'
import classes from './Home.module.scss'

const Home = () => {
  return <div className={classes.container}>Main Page</div>
}

export default Home
